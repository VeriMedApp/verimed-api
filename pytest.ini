[pytest]
# Nur das tests/-Verzeichnis einsammeln; das Root-Skript test_audit_api.py ist
# ein Beispiel-Printer und kein Test.
testpaths = tests
# Repository-Wurzel auf sys.path, damit `import app` und alembic/env.py funktionieren.
pythonpath = .
